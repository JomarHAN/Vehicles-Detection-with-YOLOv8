services:
  - type: web
    name: vehicle-detection-with-yolov8
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --reload --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000